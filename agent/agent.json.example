{
  "_comment_endpoint": "状态页站点根地址，不要带 /api，尾部斜杠可有可无",
  "endpoint": "https://uptimeflare.example.com",

  "_comment_token": "服务端 Pages 环境变量 AGENT_TOKEN 的值，必须完全一致；请勿提交到 git",
  "token": "<在此填入你的 AGENT_TOKEN>",

  "_comment_device_id": "必须与服务端 uptime.config.ts 中 devices[].id 完全一致，否则服务端返回 400",
  "device_id": "riceawa-laptop",

  "_comment_device_name": "可选，仅用于本地日志展示；页面展示名以服务端 devices[].name 为准",
  "device_name": "Riceawa 的笔记本",

  "_comment_interval": "上报间隔（秒），允许 15-120，默认 30；笔记本省电可调到 60",
  "interval": 30,

  "_comment_idle_threshold": "挂机判定阈值（秒），默认 120；建议与服务端 devices[].idleThreshold 保持一致",
  "idle_threshold": 120
}
